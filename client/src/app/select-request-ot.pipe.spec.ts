import { SelectRequestOTPipe } from './select-request-ot.pipe';

describe('SelectRequestOTPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectRequestOTPipe();
    expect(pipe).toBeTruthy();
  });
});
