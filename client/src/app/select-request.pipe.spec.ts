import { SelectRequestPipe } from './select-request.pipe';

describe('SelectRequestPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectRequestPipe();
    expect(pipe).toBeTruthy();
  });
});
