import { SelectRequestDepPipe } from './select-request-dep.pipe';

describe('SelectRequestDepPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectRequestDepPipe();
    expect(pipe).toBeTruthy();
  });
});
