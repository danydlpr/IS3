import { SelectRequestCityPipe } from './select-request-city.pipe';

describe('SelectRequestCityPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectRequestCityPipe();
    expect(pipe).toBeTruthy();
  });
});
